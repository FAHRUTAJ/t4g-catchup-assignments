i# What is a git conflict?
 A git conflict happens when two branches modify the same line of a file and git cannot automatically merge them.


#how to resolve it 

run git pull 
git marks conflicts sections
manually edit the files
git add .
git commit
